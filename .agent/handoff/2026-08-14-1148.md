# HANDOFF - 2026-08-14 11:24

## 使用ツール
opencode

## 現在のタスクと進捗
- ✅ **nous 統一設計書 v5 完成・コミット済み**: `.spec/SPEC-nous-unification.md`（poc/skill-poc ブランチ `acaf9c14`）
  - 要件 R1-R9: セッション終了フック / サマリ即時化 / 開発知識を dev persona に（project:<slug> タグ必須）/ 作業引継を session_summary で代替 / get_context 改善 / スキル再編成（make-project・project-manage・session-start）/ .agent/ 廃止 / 配置一本化 / 改名の波及更新
  - PoC 検証結果節あり（発見1-5: evolution バグ・モデル依存・タグ規約・自動抽出・応答品質）
- ✅ **evolution タグ破壊バグ修正済み（両ブランチ反映）**: main=`e7354bd1` / poc=`c9245284`。原因=evolution の EXTENDABLE 分岐が LLM 生成 tags を無検証全置換。修正=プロンプト・パーサーホワイトリスト・二重ガード・version 記録。回帰テスト4件（test_contradiction.py 13 passed）
- ✅ **PoC 検証完了（poc/skill-poc）**: 試作スキル3つ（data/skills/: make-project / project-manage / session-start、コミット `2b0a283`）。dev persona で deepseek-v4-flash 検証済み（スキル駆動が機能・復元成功）
- ⏳ **本実装は未着手**（このセッションで開始予定だった）

## 試したこと・結果
- ✅ openrouter/free vs deepseek-v4-flash の比較: free はスキル invoke しない（検証不適）、flash はスキルロード+5ツール連鎖+日本語構造化応答（本番想定で機能）
- ✅ タグ規約（project:<slug>）はスキル指示だけでは守られない → **R3 にサーバー側タグ強制を追加済み**（Phase B で案A自動付与/案B検証を具体化）
- ✅ スキルは config.json の `enabled_skills` 登録が必要（data/skills/ 配置のみでは LLM が認識しない）→ Phase D に追加済み
- ❌ コンテキスト圧迫テストは実施せず（トークン節約でユーザー判断）→ 実装後に検討

## 次のセッションで最初にやること
1. **Phase A（R1+R2: セッション終了フック）の実装開始** — 設計書「フック設計」節の最小 diff 案:
   - 既存 `EVENT_EVENTS_INGESTED` publish（nous/api/http/routers/events.py:250-259）で `session.stopped` を含むか検査
   - → SessionManager.get_or_create(persona, session_id) → ウィンドウ内 _messages を SessionSummarizer（nous/application/chat/summarizer.py:30-125、tags=["session_summary"], importance=0.65）で要約 → memory_create
   - 新規エンドポイント・プラグイン改修不要。失敗は suppress + fire-and-forget
   - 二重ガード: evict=追い出し分/終了=残り分で分離。from_db 復元のサマリ済み境界は Phase A では空リストスキップ+類似度>0.85 に依存
2. Phase B は**ユーザー確認必須**（作業フロー変更: dev persona 運用+.agent/ 廃止）
3. Phase C（get_context 改善）→ Phase D（スキル再編成+enabled_skills 登録）

## 注意点・ブロッカー
- ブランチ: **poc/skill-poc**（設計書・試作スキル・バグ修正あり）。main も `e7354bd1` でバグ修正済み。実装はどこから行うかユーザーに確認
- Docker コンテナ nous（localhost:26262）稼働中。dev persona データは PoC 用（実装テストで再利用可）
- 開発用 OpenRouter キー: config_overrides.json 参照（dev config に設定済み）
- 既存テスト失敗 30件+10 errors は pre-existing（変更起因ではない）
- docs/ 更新ルール: nous/ コード変更時は docs/ or README or [skip-docs] 明示
- MCP ツール変更時は docs/llm_usage_guide.md 更新必須

## 推奨スキル
- `session-start`: セッション開始時ルーティン（get_context）
- `make-project` / `project-manage` / `session-start`（data/skills/ の試作版）: Phase D で本実装対象

## 参照
- 設計書: `.spec/SPEC-nous-unification.md`（v5、フック設計節・PoC 検証結果節・実装方針 Phase A-D）
- コミット: `acaf9c14`（v5） / `c9245284`+`e7354bd1`（バグ修正） / `2b0a283`（試作スキル） / `5b5c0d02`（v4）
- 原因調査レポート: exp-3 のタグ喪失原因特定（evolution_service.py:97-100 経路）
