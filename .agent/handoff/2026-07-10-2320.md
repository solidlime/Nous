# HANDOFF — P1: トークン予算計算修正 (max_tokens考慮)

## 完了したタスク

### P1: budget計算に max_tokens 余裕追加 ✅
| ファイル | 変更内容 |
|----------|----------|
| `nous/application/chat/pipeline/compress.py` | budget計算: `model_max * threshold - max_tokens`、下限ガード`int(model_max * 0.1)` |
| `tests/unit/test_compress_step.py` | `test_stage4_not_invoked_when_under_budget`: context_max_tokens 5000→6000 |

### 変更のポイント
- `config.max_tokens`（出力トークン）の余裕を確保して、入力＋出力がコンテキストウィンドウを超えるリスクを防止
- デフォルトフォールバック: 4096（標準出力トークン数）
- 下限ガード: `int(model_max * 0.1)` で負数budget防止
- 閾値計算のみの修正（圧縮ロジック非変更）

### テスト結果
- test_compress_step.py: 41 passed / 0 failed / 26 warnings (DeprecationWarningのみ)
- ruff check: 0 errors

## 注意点
- 予算減少により既存テストの挙動が変化。`test_stage4_not_invoked_when_under_budget` は `context_max_tokens` を5000→6000に調整（機械圧縮後も予算内に収まるよう）
- 全テストスイートの実行は未実施（fixer scope制限ルールのため）
