# HANDOFF — 2026-08-01 01:10

## セッションステータス
- **作業中断**: 画像生成パイプラインの全面リライト計画が確定済み。実装は次セッションで開始する。
- 未コミット変更なし（前回のリコンサイル時点で `git status` clean を確認。以降の変更は全てコミット済み）。

## 使用ツール
OpenCode

## 直近セッションの完了内容（2026-07-25〜08-01, 50+ commits, all on main）

### ComfyUI 画像生成（最大のテーマ群）
- `image_gen_mode` (t2i/i2i) 追加、i2i 参照画像アップロード UI + テストエンドポイント対応
- サイズ指定を名前付きプリセットシステムに置換（`52c836d`）
- ワークフローテンプレートモード追加（IP-Adapter は追加→削除で revert、`60106ea`）
- `NOUS` タグ注入（_meta.title 走査）・`NOUS:display` ノードフィルタ・定数ノード対応・Power Lora Loader オブジェクト形式修正
- 生成画像に node id/title 付与、sampler/scheduler オプション拡充（sampler 21種 / scheduler 9種）
- デフォルトワークフロー `data/workflows/default_node.json` 同梱（git追跡、`.gitignore` 例外）
- LoRA 焼き込み無効化（node 243 の lora_1..5 を `{"on": false}` に）

### Overview / メモリダッシュボード UI
- ゲーム風ステータス画面（portrait hero + コンパクト）へ再設計
- タブ型 persona status panel（Main/Equipment/Goals）、Profile/Emotion/Equipment/Body カード統合
- メモリダッシュボード統計・ブロック・チャート表示（memories tab へ移設）

### タイムコンテキスト & 応答検証
- `_build_time_context` を `<time_context>` タグでラップ + anti-echo 指示（`191704b`）
- タイムスタンプエコー・XML タグリーク検出を応答検証に追加

### TTS (irodori)
- LLM キャプション生成（`irodori_caption_llm_enabled/model` config、UI、感情強度計算修正）
- コンテキスト注入最適化（メモリタグ検索廃止 → PersonaState 直接参照）

---

## 🔴 次のセッションで最初にやること: 画像生成パイプライン全面リライト

### 背景（発生したバグ）
動的ビルド `_build_workflow()`（comfyui.py L492-650）で **ノードID衝突** が発生:
- LoRA チェーンが `next_id=12` から採番（L542, L550）
- img2img 時 `nodes["12"] = VAEEncode`（L588-594）が最初の LoRA ノードを上書き
- → ComfyUI 側で `LoraLoader 13: tuple index out of range`（ユーザーが実ログで確認）

### ユーザー決定（確定済み・仕様書あり）
1. **テンプレートモードを正規に維持**（デフォルト `workflows/default_node.json` 添付のまま）
2. **動的ビルド `_build_workflow()` は廃止・全削除**（テンプレート空設定時は ValueError）
3. **生成モードは i2i 限定** — t2i/i2i 選択肢削除、UI から mode select 除去、`image_gen_mode` config 削除
4. **speed_lora は不要** — 設定・UI（display:none の L194-198）・オーバーライドロジック撤去

### 仕様書
**`.spec/SPEC-image-gen-rewrite.md`** に全変更仕様を保存済み。必ず読むこと。

### 変更範囲（6ファイル + テスト）
1. `nous/infrastructure/image_gen/comfyui.py`: speed_lora パラメータ削除、`workflow_template` 必須化（空→ValueError）、generate() から動的ビルド分岐削除、`_build_workflow()` 全削除、speed_lora オーバーライド削除、負プロンプト3重複を1定数に集約
2. `nous/domain/tool_config.py`: `image_gen_mode` / `speed_lora_path` / `speed_lora_weight` / `speed_lora_method` 削除、`workflow_template` デフォルト維持
3. `nous/application/chat/tools/builtin.py`: mode 分岐削除（i2i固定）、speed_lora 配線削除、`reference.png` 常時読込（なければ明示エラー、t2i フォールバック廃止）、ハードコード checkpoint 直書き削除、ImageGenConfig 中間オブジェクト削除or有効活用
4. `nous/api/http/routers/image_gen.py`: test_image_gen() の mode 分岐・speed_lora 削除、参照画像常時読込、checkpoint 直書き削除
5. UI 3ファイル（`chat_sidebar_media.py` / `chat-settings.js` / `chat-settings-image.js`）: mode select / speed_lora UI 削除、参照画像アップロード UI 常時表示
6. `tests/unit/test_comfyui_provider.py`: `_build_workflow` 系テスト削除・整理、`workflow_template` 空→ValueError テスト追加、既存テンプレート注入/NOUS/Power Lora テスト維持

### 検証計画
- `pytest tests/unit/test_comfyui_provider.py` 全パス / `py_compile` / `ruff`
- サーバー再起動 → `POST /api/chat/herta/image-gen/test` で実生成確認
- コミット方針: 破壊的変更のため複数コミット分割可、docs 未更新は `[skip-docs]` 明示

### 委譲先（再利用可）
- **fix-1 / ses_0479f32b0ffeV7WWAVvSQRpzux**（comfyui.py・builtin.py・image_gen.py・base.py・テストのコンテキスト保持済み）→ 実装委譲に最適
- lib-1 / ses_04753a3acffeu0Re6VCq4ks5H9（Power Lora Loader 形式調査済み）

---

## 環境メモ
- サーバー: docker `nous:dev` コンテナ稼働中（`docker restart nous` で再起動）、ポート 26262、ライブマウント `./nous→/app/nous` `./data→/data`
- herta persona の画像生成設定は `data/persona/herta/config.json`（checkpoint=JANKUTrainedChenkinNoobai_v777、comfyui url=http://192.168.50.150:8188、workflow_template フィールドなし→デフォルト適用）
- `workflow_template` は data_root 相対解決

## 注意点・ブロッカー
- MEMORY.md は 200 行上限超過（202行）。次の知識追加時にアーカイブして新規作成すること
- `stash@{0}`（fc7b696 重複）は内容確認後に drop 検討
- 既知の LSP 型エラー: builtin.py:127（`__setitem__` に list を str 代入）、image_gen.py:166（str に .read）— リライトで解消予定
