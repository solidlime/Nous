# ==========================================
# Nous v3 — 開発・テスト用依存関係
# ==========================================
# 本番イメージには含めない。
# インストール: pip install -r requirements-prod.txt -r requirements-dev.txt

# Testing
pytest>=9.1.1
pytest-asyncio>=1.4.0
pytest-cov>=7.1.0
pytest-mock>=3.15.1
pytest-timeout>=2.4.0

# E2E Testing (Playwright)
playwright>=1.40.0
pytest-playwright>=0.9.0

# Benchmarking
pytest-benchmark>=5.2.3

# Static Analysis & Linting
# NOTE: exact pins — CI とローカルのツールドリフトを防ぐ。
# バージョンを上げるときは mypy-baseline.yaml の再生成もセットで行うこと。
ruff==0.16.4
mypy==2.3.1
mypy-baseline==0.7.4
bandit==1.9.4
