# ==========================================
# memory-mcp v2 dependencies
# ==========================================

# Embedding & Reranker (ONNX Runtime — PyTorch-free)
onnxruntime>=1.18.0
tokenizers>=0.21.0
huggingface_hub>=0.20
sentencepiece>=0.1.99  # Required for cl-nagoya/ruri-v3-30m tokenizer

# MCP Server
mcp>=1.0.0

# Data Validation & Settings
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0

# Vector Store
qdrant-client>=1.8.2

# Numerical
numpy>=1.24.0

# E2E Testing (Playwright)
playwright>=1.40.0
pytest-playwright>=0.4.0

# Japanese tokenizer for smart search (optional, falls back to regex-based expansion)
# janome>=0.5
bandit>=1.7
mypy>=1.9

# Benchmarking
pytest-benchmark>=4.0

# LLM Providers (Chat feature)
anthropic>=0.40.0
openai>=1.50.0
# Docker SDK (sandbox sibling-container detection)
docker>=7.0.0

# Japanese NER (entity extraction)
sudachipy>=0.6.0
sudachidict_core>=20240716

# PDF/Document Parsing (read_pdf tool)
PyMuPDF>=1.24.0
pdfplumber>=0.11.0
pytesseract>=0.3.10
