# Gemini用プロジェクトガイド

このプロジェクトは **CLAUDE.md** を一次ソースとしたドキュメント設計を採用しています。
Gemini で作業する場合は、最初に `CLAUDE.md` を読み込んでから作業を開始してください。

## 差分

- セッション開始時に `AGENTS.md` を必ず読み込むこと
- `AGENTS.md` 読み込み後、`CLAUDE.md` を読み込んでプロジェクト構成・ツールAPI・設計ルールを把握すること
- 詳細な開発ルール・コミットルールは `AGENTS.md` に記載
- 全てのドキュメントは `docs/` 配下に集約
- 作業開始前は `.spec/` 配下の PLAN → SPEC → TODO フローを遵守
